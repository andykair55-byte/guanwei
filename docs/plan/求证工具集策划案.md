# 见微「求证工具集」策划案

> 版本：v3.2 | 更新日期：2026-07-16 | 状态：修订砍掉清单与代码现状对齐
>
> **v3.2 修订说明**：
> v3.1 曾声明"已砍掉"反向搜图/洗稿检测/多源验证 3 个工具，并将情绪检测归入 trae宝。
> 代码审查发现：上述 4 个工具的页面与路由**实际全部存在**（见 `src/router/routes.ts` 第 127-133 行）。
> 本次修订将"已砍掉"清单更正为"已保留但标注为实验性工具"，并补充"工具清单现状"一节。
>
> **v3.1 架构审查调整说明（保留）**：
> - LLM文字求证：**已嵌入工作间**（`/agent-world`），不再独立页面
> - 情绪检测：策划上**已归入trae宝**，但独立工具页 `/tools/emotion` 实际仍保留——与 trae宝情绪检测能力存在重叠，待后续整合
> - EXIF分析：**保持独立页** `/tools/exif`
> - 时间线构建：**保持独立页** `/tools/timeline`，关联热点系统
> - 反向搜图 / 洗稿检测 / 多源验证：**v3.1 声明砍掉，但代码未删，本次更正为"实验性工具"**

---

## 一、模块定位

**求证工具集是观微的AI辅助验证工具集——共 6 个工具页面，均通过 `/tools/*` 路由对外暴露。其中 EXIF分析、时间线构建为正式工具；反向搜图、洗稿检测、多源验证为实验性工具（前端 mock 演示，未接真实后端能力）；情绪检测与 trae宝功能重叠，待整合。LLM文字求证已嵌入工作间创作流程，不在本工具集独立页面。**

---

## 二、MVP范围定义

### 做什么

| 功能 | 说明 | 状态 |
|------|------|------|
| EXIF分析 | 图片上传后解析元数据（设备、时间、GPS、编辑历史） | 正式工具，独立页 `/tools/exif` |
| 时间线构建 | 构建事件时间线，关联热点系统 | 正式工具，独立页 `/tools/timeline` |
| 反向搜图 | 上传图片检索网络来源、首次出现时间、相似度 | 实验性工具（mock），独立页 `/tools/reverse-image` |
| 洗稿检测 | 两段文本比对，标记命中段并给出相似度 | 实验性工具（mock），独立页 `/tools/plagiarism` |
| 多源验证 | 多信源立场/可信度对比，标注一致性 | 实验性工具（mock），独立页 `/tools/multi-source` |
| 情绪检测 | 拆解话术结构，识别情绪操控与修辞陷阱 | 与 trae宝重叠，独立页 `/tools/emotion` 保留 |
| 工具入口页 | 统一工具箱入口，选择工具后进入对应页面 | 需重构（6个工具） |

### 工具归属调整（架构审查后）

| 工具 | 归属 | 说明 |
|------|------|------|
| LLM文字求证 | **已嵌入工作间** | 创作流程中声明检测→标记→一键核查，复用此能力，不再独立页面 |
| 情绪检测 | **与 trae宝重叠** | 独立页 `/tools/emotion` 保留；trae宝（小微）内亦含 Brain 情绪检测能力，二者待整合 |
| EXIF分析 | **保持独立页** | 图片元数据分析，不嵌入创作流程 |
| 时间线构建 | **保持独立页** | 关联热点系统，构建事件时间线 |
| 反向搜图 | **实验性保留** | v3.1 声明砍掉，代码未删；前端 mock 演示，未接图片搜索引擎 |
| 洗稿检测 | **实验性保留** | v3.1 声明砍掉，代码未删；前端 mock 演示，未接 embedding 模型 |
| 多源验证 | **实验性保留** | v3.1 声明砍掉，代码未删；前端 mock 演示，未接爬取/比对后端 |

### 实验性工具清单（原"砍掉清单"更正）

| 工具 | 状态 | 实现现状 | 后续方向 |
|--------|------|----------|----------|
| 反向搜图 | 实验性保留 | `ReverseImageSearch.tsx` 含 3 个预设图片 mock（城市夜景/山川风景/网红猫咪），无真实图片搜索 | 接入第三方图片搜索 API 后转正，或确认无资源后下线 |
| 洗稿检测 | 实验性保留 | `PlagiarismChecker.tsx` 含 2 个预设样本 mock（明显洗稿/精修洗稿），无真实 embedding 比对 | 接入 embedding 模型后转正，或下线 |
| 多源验证 | 实验性保留 | `MultiSourceVerify.tsx` 含 5 个信源 mock（新华社/当事人/知乎/匿名论坛/央视），无真实爬取 | 复杂度过高，建议长期保留 mock 演示形态 |
| 情绪检测 | 重叠保留 | `EmotionDetector.tsx` 真实调用 `analyzeEmotion` service；trae宝内 Brain 能力亦调用同一 service | 与 trae宝整合，二选一保留 |
| 智能路由 | 不做 | 工具数量增加但仍有界，用户手动选择 | — |
| 统一工作台（多列布局） | 不做 | 单工具单页足够 | — |
| 聚合报告+可信度评分 | 不做 | 依赖实验性工具，无法聚合 | — |
| 工具使用统计 | 不做 | MVP无必要 | — |
| 验证历史 | 不做 | 可后续迭代 | — |

> 注：原 v3.0 中"时间线构建归入热点系统"的决策已调整——时间线构建现在**保持独立页** `/tools/timeline`，并关联热点系统。

### 工具清单现状（v3.2 新增）

> 数据来源：`src/router/routes.ts` 第 127-133 行 + 各工具页源码。

| 工具 | 路由 | 页面文件 | 数据来源 | 状态 |
|------|------|----------|----------|------|
| EXIF分析 | `/tools/exif` | `src/pages/ExifAnalyzer.tsx` | 真实 EXIF 解析 | 正式 |
| 时间线构建 | `/tools/timeline` | `src/pages/TimelineBuilder.tsx` | 真实数据 | 正式 |
| 反向搜图 | `/tools/reverse-image` | `src/pages/ReverseImageSearch.tsx` | mock（3 个预设图片） | 实验性 |
| 洗稿检测 | `/tools/plagiarism` | `src/pages/PlagiarismChecker.tsx` | mock（2 个预设样本） | 实验性 |
| 多源验证 | `/tools/multi-source` | `src/pages/MultiSourceVerify.tsx` | mock（5 个信源） | 实验性 |
| 情绪检测 | `/tools/emotion` | `src/pages/EmotionDetector.tsx` | 真实 `analyzeEmotion` service | 与 trae宝重叠 |

**与 trae宝（小微）的功能重叠说明**：

`EmotionDetector.tsx` 与 `src/components/TraeBot.tsx` 中的 Brain 情绪检测能力均调用 `src/services/emotionAnalysis.ts` 的 `analyzeEmotion`，能力完全重叠：

- `EmotionDetector.tsx`：独立工具页形态，输入文本→展示情绪分析卡片（风险等级、话术统计、客观性摘要），适合深度分析
- `TraeBot.tsx`：右下角悬浮 agent 形态，选中文本→在聊天面板内展示情绪结果，适合轻量即时分析

两者共用底层 service、面向不同交互场景。建议保留其一作为主入口，另一个降级或移除。整合决策待定，本次修订暂保留双形态。

---

## 三、技术架构

### 3.1 前端

```
src/
  pages/
    ExifAnalyzer.tsx             # EXIF分析页（真实解析，保留独立）
    TimelineBuilder.tsx          # 时间线构建页（保持独立，关联热点）
    ReverseImageSearch.tsx       # 反向搜图页（实验性，mock 数据）
    PlagiarismChecker.tsx        # 洗稿检测页（实验性，mock 数据）
    MultiSourceVerify.tsx        # 多源验证页（实验性，mock 数据）
    EmotionDetector.tsx          # 情绪检测页（与 trae宝重叠，保留）
    # LLMVerifyPage.tsx          # 已移除——LLM文字求证嵌入工作间
  components/
    tools/
      ToolCard.tsx               # 工具箱入口卡片
      VerifyProgress.tsx         # 分析进度指示器（EXIF用）
  stores/
    toolsStore.ts                # 工具状态store
  services/
    toolsService.ts              # API调用封装（仅EXIF）
    emotionAnalysis.ts           # 情绪检测service（EmotionDetector + TraeBot 共用）
```

> 注：LLM文字求证的相关组件（LLMVerifyInput/LLMVerifyResult）已迁移至工作间 `components/create/ClaimVerifier.tsx`，本工具集不再维护。
> 情绪检测相关逻辑在 trae宝 `TraeBot.tsx` 内调用，但独立页 `EmotionDetector.tsx` 同样保留。

### 3.2 后端

```
backend/
  app/
    routers/
      tools.py                   # 工具API路由
    services/
      exif_service.py            # EXIF分析（已有）
      emotion_service.py         # 情绪检测（已有）
      llm_verify_service.py      # LLM文字求证（核心新服务）
    utils/
      cors_proxy.py              # CORS代理（解决跨域问题）
```

### 3.3 CORS问题解决方案

**问题：** 用户输入外部URL后，前端直接fetch会因CORS被浏览器拦截。

**方案：后端代理。**

```python
# backend/app/utils/cors_proxy.py
from fastapi import APIRouter, HTTPException
import httpx

router = APIRouter()

@router.post("/api/tools/fetch-url")
async def fetch_url(url: str):
    """
    后端代理抓取外部URL内容，解决CORS问题。

    限制：
    - 只允许http/https协议
    - 响应体最大1MB
    - 超时10秒
    - 每用户每分钟最多10次请求
    """
    if not url.startswith(("http://", "https://")):
        raise HTTPException(400, "只支持http/https链接")

    async with httpx.AsyncClient(timeout=10, follow_redirects=True) as client:
        try:
            resp = await client.get(url, headers={"User-Agent": "JianWei/1.0"})
            content = resp.text[:1_000_000]  # 限制1MB
            return {"content": content, "status": resp.status_code}
        except httpx.TimeoutException:
            raise HTTPException(504, "抓取超时")
        except Exception as e:
            raise HTTPException(500, f"抓取失败: {str(e)}")
```

### 3.4 LLM文字求证的完整实现路径（已迁移至工作间）

> **架构审查调整**：LLM文字求证已**嵌入工作间**（`/agent-world`）创作流程，不再作为独立工具页。
> 以下实现细节保留作为工作间 ClaimVerifier 组件的参考实现，工作间复用此流程。
> 详细工作间设计见 `工作间策划案.md` 4.3节 LLM求证嵌入。

#### 3.4.1 输入

用户输入：文本（直接输入）或 URL（后端代理抓取后提取正文）

#### 3.4.2 输入预处理

```python
def preprocess_input(user_input: str, input_type: str) -> str:
    """
    预处理用户输入，构造LLM的输入文本。

    - 直接文本：截断到2000字（约3000 token，留足prompt空间）
    - URL内容：用httpx抓取后，提取HTML正文（readability算法或简单去标签），截断到2000字
    """
    if input_type == "url":
        # 调用cors_proxy抓取
        raw_html = fetch_url_content(user_input)
        # 简单提取正文：去除script/style标签，取body文本
        text = extract_text_from_html(raw_html)
    else:
        text = user_input

    # 截断
    return text[:2000]
```

#### 3.4.3 Prompt构造

```python
VERIFY_PROMPT = """你是一个信息求证助手。请分析以下信息的可信度。

## 分析要求
1. 判断该信息是"基本属实"、"部分属实"、"难以判断"还是"基本不实"
2. 列出支持该信息的关键论据（最多3条）
3. 列出质疑该信息的关键疑点（最多3条）
4. 给出1-5星的可信度评级

## 输出格式（严格JSON）
{
  "verdict": "基本属实|部分属实|难以判断|基本不实",
  "stars": 1-5,
  "supporting": ["论据1", "论据2", "论据3"],
  "questioning": ["疑点1", "疑点2", "疑点3"],
  "summary": "50字以内的总结"
}

## 待分析信息
{text}"""
```

#### 3.4.4 Groq调用

```python
from groq import Groq

client = Groq(api_key=settings.GROQ_API_KEY)

def call_llm_verify(text: str) -> dict:
    """
    调用Groq进行文字求证。

    Token预算：
    - 输入：~3000 token（2000字中文 + prompt模板）
    - 输出：~500 token
    - 单次总计：~3500 token
    - 每用户每日限10次 = 35000 token/日，远在免费额度内
    """
    response = client.chat.completions.create(
        model="llama-3.3-70b-versatile",
        messages=[{"role": "user", "content": VERIFY_PROMPT.format(text=text)}],
        temperature=0.3,  # 低温度，结果更稳定
        max_tokens=500,
        response_format={"type": "json_object"},  # 强制JSON输出
    )
    return json.loads(response.choices[0].message.content)
```

#### 3.4.5 结果解析与展示

```
LLM返回JSON → 后端校验字段完整性 → 返回前端 → LLMVerifyResult组件渲染

展示内容：
┌──────────────────────────────┐
│ 可信度: ★★★☆☆                │
│ 判定: 部分属实                │
│                              │
│ 总结: 该信息在部分细节上得到   │
│ 多方验证，但关键数据缺乏      │
│ 权威来源支撑。                │
│                              │
│ 支持论据:                    │
│ · 新华社相关报道提及此事      │
│ · 多位当事人社交媒体发言一致  │
│                              │
│ 质疑疑点:                    │
│ · 核心数据未标注来源          │
│ · 时间线与官方通报有出入      │
│                              │
│ [提交为瓜田佐证]              │
└──────────────────────────────┘
```

#### 3.4.6 延迟预期

| 环节 | 耗时 |
|------|------|
| URL抓取（如适用） | 1-3秒 |
| Groq API调用 | 2-5秒（llama-3.3-70b在Groq上通常2-3秒） |
| 结果解析+渲染 | <0.1秒 |
| 总计 | 3-8秒 |

前端需展示进度指示器（`VerifyProgress.tsx`），避免用户焦虑。

---

## 四、实现路径

### 阶段1：LLM文字求证核心

| 步骤 | 交付物 | 验证方式 |
|------|--------|----------|
| 后端LLM求证接口 | `POST /api/tools/verify` 接收文本返回分析结果 | curl测试，返回正确JSON |
| 后端URL代理接口 | `POST /api/tools/fetch-url` | curl测试，能抓取外部页面 |
| 前端LLM求证页 | `LLMVerifyPage.tsx` 含输入+进度+结果展示 | 页面可用，结果正确展示 |
| 进度指示器 | `VerifyProgress.tsx` | 请求期间展示分析进度 |

### 阶段2：工具箱入口 + 已有工具整合

| 步骤 | 交付物 | 验证方式 |
|------|--------|----------|
| 工具箱入口页 | `ToolsIndexPage.tsx` 6个工具卡片（区分正式/实验性） | 点击跳转正确 |
| EXIF分析页检查 | 确认已有功能正常 | 上传图片能解析 |
| 情绪检测页检查 | 确认已有功能正常 | 输入文本能分析 |
| 实验性工具标注 | 反向搜图/洗稿检测/多源验证卡片标注"实验性" | mock 数据可演示 |

### 阶段3：结果提交佐证

| 步骤 | 交付物 | 验证方式 |
|------|--------|----------|
| 提交佐证按钮 | `LLMVerifyResult.tsx` 中增加"提交为佐证"按钮 | 点击后跳转佐证提交页 |
| 数据预填充 | 验证结果自动填入佐证表单 | 佐证内容包含验证结论 |

---

## 五、状态流转

### 5.1 LLM求证请求状态

```
IDLE（空闲）→ FETCHING（抓取URL中）→ ANALYZING（LLM分析中）→ DONE（完成）
                                                           → ERROR（失败）
```

### 5.2 前端UI状态

| 状态 | UI表现 |
|------|--------|
| IDLE | 输入框可用，"开始验证"按钮高亮 |
| FETCHING | 输入框锁定，进度条显示"正在抓取链接内容..." |
| ANALYZING | 进度条显示"AI分析中，请稍候..." |
| DONE | 展示结果卡片，"提交为佐证"按钮可用 |
| ERROR | 展示错误信息，"重试"按钮 |

---

## 六、异常场景应对

### 6.1 LLM超时

| 场景 | 处理 |
|------|------|
| Groq API >15秒无响应 | 后端断开连接，返回504，前端提示"分析超时，请稍后重试" |
| 连续3次超时 | 前端提示"AI服务暂时不可用，请使用其他工具" |

### 6.2 LLM返回格式错误

| 场景 | 处理 |
|------|------|
| 返回非JSON | 后端catch解析异常，返回500，前端提示"分析结果解析失败，请重试" |
| JSON缺少必要字段 | 后端校验字段完整性，缺失则用默认值填充（verdict="难以判断", stars=3） |
| response_format不生效 | 备选方案：在prompt中强调JSON格式，并在后端做正则提取 |

### 6.3 输入过长

| 场景 | 处理 |
|------|------|
| 文本超过2000字 | 前端截断并提示"输入过长，已截取前2000字" |
| URL页面内容过大 | 后端提取正文后截断到2000字 |

### 6.4 URL抓取失败

| 场景 | 处理 |
|------|------|
| 目标网站反爬 | 返回错误提示"无法抓取该链接，请尝试直接粘贴文本" |
| 非HTML内容（PDF/视频） | 提示"暂不支持该类型链接，请粘贴文字内容" |
| 链接无效 | 提示"链接无法访问，请检查后重试" |

### 6.5 Groq API额度耗尽

| 场景 | 处理 |
|------|------|
| 429 Rate Limit | 后端catch，前端提示"使用次数已达上限，明日再试" |
| 月度额度接近耗尽 | 后端监控用量，达到80%时限制每用户每日3次 |

---

## 七、上线后运营

| 运营动作 | 频率 | 说明 |
|----------|------|------|
| 监控LLM用量 | 每日 | 检查token消耗，避免超额 |
| 收集用户反馈 | 每周 | 验证结果是否有帮助 |
| 调整prompt | 按需 | 根据反馈优化分析质量 |
| 工具使用数据 | 每周 | 哪个工具用得多，指导后续迭代 |
| 实验性工具去留评审 | 每月 | 评估反向搜图/洗稿/多源验证是否转正或下线 |
| 情绪检测整合决策 | 待定 | 与 trae宝情绪检测能力整合方案评审 |

---

## 八、资源需求

| 资源 | 数量 | 说明 |
|------|------|------|
| 开发人力 | 1前端 + 1后端 | — |
| Groq API | 每用户每日10次LLM求证 | 每次约3500 token，10用户日消耗350k token |
| 后端代理 | httpx库 | 已有依赖，无额外成本 |
| exifr | 前端库 | 已集成 |
| 情绪检测 | 已有实现 | `emotionAnalysis.ts`，无额外成本 |
| 实验性工具 | mock 数据 | 无后端成本，转正时另评估 |
